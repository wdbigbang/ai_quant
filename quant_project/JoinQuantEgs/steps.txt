1. 当前代码的总体设计思路是使用一个 总控决策模块（StrategyManager类） 来控制多个基于 策略基类（BaseStrategy） 的交易策略 SwingStrategy 和 ETFRotationStrategy。其中 根据市场行情调整策略的资金池以及不同策略的股票 + 现金总数严格不超过所属资金池比例 + 每个策略的买卖策略不能影响其他策略 + 每个策略的收益、胜率、股票/基金标签等严格独立计算且互不影响 是总体设计原则，不可变更。
2. 先阅读 ai_debug.md 文档，了解工程详情和进度，并检查 调试信息栏 是否有信息。
3. 如果 调试信息栏 有信息说明有为解决的代码问题，请优先解决，并让我调试确认；没有则调过该步骤。
4. 如果进度信息与 ai_debug.py 文件中的实际代码完成度不一致，以代码文件情况为准。
5. 严格按照每一次只完成一个步骤的代码实现，保证留下足够的上下文，且一定要注意numpy pandas talib 等库的用法，不要有低级语法错误。
6. 不需要一次性完成该项目，建议按阶段完成，来不及完成的函数建议做一个假输出便于调试，并等我调试确认。